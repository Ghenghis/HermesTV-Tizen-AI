{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "hermestv:commands:update_background",
  "title": "update_background params",
  "type": "object",
  "required": ["pack_id"],
  "additionalProperties": false,
  "properties": {
    "pack_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{1,48}[a-z0-9]$",
      "description": "Background pack ID (kebab-case). 12 packs defined in docs/05_THEME_BACKGROUND_ENGINE_CONTRACT.md."
    },
    "motion": {
      "type": "boolean",
      "description": "Override motion on/off for this background. If not provided, inherits profile motion setting."
    }
  }
}
