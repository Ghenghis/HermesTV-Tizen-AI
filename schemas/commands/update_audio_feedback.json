{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "hermestv:commands:update_audio_feedback",
  "title": "update_audio_feedback params",
  "description": "Toggle audio feedback (TTS on focus/navigation). For mom_tv in Mom Mode: always ON — backend rejects false for mom_tv.",
  "type": "object",
  "required": ["enabled"],
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "type": "boolean"
    }
  }
}
