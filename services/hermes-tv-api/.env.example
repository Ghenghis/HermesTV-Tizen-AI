PORT=3001
NODE_ENV=development
# HERMESTV_VAULT_PATH — set this to G:\private\ on the workstation. Agents never read this path.
# HERMESTV_VAULT_PATH=/path/to/vault
# AZURE_TTS_REGION — Azure region for TTS service
# AZURE_TTS_REGION=eastus
# (Never put actual keys in this file or in environment)
