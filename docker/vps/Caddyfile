hermestv.local {
    # Phase 1.5: 3001 → 3011 (VPS port conflict with Dave's next-server).
    # See docs/20_VPS_PHASE_1_AUDIT_FINDINGS.md.
    reverse_proxy hermes-tv-api:3011
    tls internal
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy no-referrer
    }
}
