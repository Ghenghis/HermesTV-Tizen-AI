# /tasks

Break an approved DaveTV plan into small testable tasks using
`specs/templates/tasks.md`.

Rules:

- Each task must be independently reviewable.
- Each task must list files and proof.
- Provider/auth/VPS/playback work must include integration or live proof.
- Split parallel agent work by disjoint file ownership.
